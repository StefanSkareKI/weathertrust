[build]
builder = "nixpacks"

[deploy]
startCommand = "node --experimental-sqlite .next/standalone/server.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"

[environments.production.variables]
PORT = "3000"
HOSTNAME = "0.0.0.0"
