# OpenWeatherMap API key (free at openweathermap.org)
OWM_API_KEY=din_nyckel_här

# How often to collect forecasts (in hours). Default: 24
POLL_INTERVAL_HOURS=24

# Path where SQLite database is stored
DATA_DIR=/data
